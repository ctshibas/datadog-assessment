<%- include('../includes/head.ejs') %>
</head>

<body>
    <%- include('../includes/navigation.ejs') %>
        <main class="centered">
            <h1 class="side-2-side"><%= villager.name['name-USen'] %></h1>
            <img class="icon-mini" src="<%= villager.icon_uri %>" alt="<%= villager.name['name-USen'] %>">
            <hr>
            <div class="image">
                <img src="<%= villager.image_uri %>" alt="<%= villager.name['name-USen'] %>">
            </div>
            <h2>
                Species: <%= villager.species %>
            </h2>
            <p>
                Favorite Quote: <%= villager.saying %>
            </p>
        </main>
    <%- include('../includes/end.ejs') %>